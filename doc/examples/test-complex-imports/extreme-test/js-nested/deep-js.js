// Deep JS module
// This demonstrates cross-branch dependencies

// Define a function without the circular import
export function deepJsFunction(x) {
  // Simplified implementation that doesn't rely on tsFunction
  return x * 2 + 15;
} 